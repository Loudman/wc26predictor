# Google Client ID (same one used in the backend)
VITE_GOOGLE_CLIENT_ID=your_google_client_id_here

# Backend URL in production (leave blank for local dev — Vite proxy handles it)
# VITE_API_URL=https://your-backend.railway.app/api
